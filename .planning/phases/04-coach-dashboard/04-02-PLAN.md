---
phase: 04-coach-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/coach/user-card-grid.tsx
  - src/components/coach/user-card.tsx
  - src/components/coach/habit-heatmap.tsx
  - src/app/(coach)/dashboard/page.tsx
autonomous: true
requirements: [COCH-01, COCH-02, COCH-03, COCH-04, COCH-05]

must_haves:
  truths:
    - "Coach sees a grid of user cards sorted by engagement risk with green/yellow/red heart indicators"
    - "Coach can expand a user card inline to see quest completion heatmap for past 7 days"
    - "Coach can send a nudge directly from an expanded user card"
    - "Users with 30%+ engagement drop show red pulsing heart and appear first in the grid"
    - "Nudge button is disabled if coach already nudged that user today"
  artifacts:
    - path: "src/components/coach/user-card-grid.tsx"
      provides: "Client component managing grid layout and expand state"
      min_lines: 30
    - path: "src/components/coach/user-card.tsx"
      provides: "Individual user card with summary and expandable detail"
      min_lines: 50
    - path: "src/components/coach/habit-heatmap.tsx"
      provides: "7-day habit completion grid (rows=habits, cols=Mon-Sun)"
      min_lines: 20
    - path: "src/app/(coach)/dashboard/page.tsx"
      provides: "Server component that fetches data and renders UserCardGrid"
      min_lines: 15
  key_links:
    - from: "src/app/(coach)/dashboard/page.tsx"
      to: "src/lib/queries/coach.ts"
      via: "imports getCoachDashboardData"
      pattern: "getCoachDashboardData"
    - from: "src/components/coach/user-card.tsx"
      to: "src/lib/actions/coach.ts"
      via: "imports fetchUserDetail for lazy heatmap load"
      pattern: "fetchUserDetail"
    - from: "src/components/coach/user-card.tsx"
      to: "src/lib/actions/social.ts"
      via: "imports sendNudge for coach nudge action"
      pattern: "sendNudge"
    - from: "src/components/coach/user-card-grid.tsx"
      to: "src/components/coach/user-card.tsx"
      via: "renders UserCard for each user in grid"
      pattern: "UserCard"
---

<objective>
Build the coach dashboard UI: a responsive grid of user cards with risk heart indicators, accordion-style expand to show habit heatmap, and inline nudge capability. Replaces the placeholder dashboard page with the full coach experience.

Purpose: This is the entire visual and interactive layer of the coach dashboard. Coach logs in and immediately sees their community's engagement health at a glance.
Output: Four files -- three components and the dashboard page -- delivering the complete COCH-01 through COCH-05 experience.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-coach-dashboard/04-CONTEXT.md
@.planning/phases/04-coach-dashboard/04-RESEARCH.md
@.planning/phases/04-coach-dashboard/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (src/lib/queries/coach.ts) -->
```typescript
export interface CoachUserCard {
  userId: number;
  displayName: string;
  currentStreak: number;
  weeklyXp: number;
  lastActiveDate: string | null;
  thisWeekLogs: number;
  lastWeekLogs: number;
  engagementRatio: number;
  riskLevel: "green" | "yellow" | "red";
  nudgedToday: boolean;
}

export interface HabitDay {
  habitName: string;
  habitIcon: string | null;
  days: { date: string; completed: boolean }[];
}

export async function getCoachDashboardData(coachId: number): Promise<CoachUserCard[]>;
export async function getUserHabitHeatmap(userId: number): Promise<HabitDay[]>;
```

```typescript
// src/lib/actions/coach.ts
export async function fetchUserDetail(userId: number): Promise<HabitDay[]>;
```

<!-- From existing code -->
```typescript
// src/lib/actions/social.ts
export async function sendNudge(friendId: number): Promise<{ sent: boolean }>;
```

```typescript
// src/components/ui/pixel-card.tsx
export function PixelCard({ as, className, children, ...props }: PixelCardProps): JSX.Element;
```

```typescript
// src/components/ui/pixel-button.tsx
export function PixelButton({ variant, size, className, children, ...props }: PixelButtonProps): JSX.Element;
// Variants: "primary" | "secondary" | "danger"
// Sizes: "sm" | "md" | "lg"
```

```typescript
// src/lib/auth.ts
export async function requireAuth(): Promise<SessionData>;
// SessionData: { userId: number; role: "player" | "coach"; displayName: string; isLoggedIn: boolean; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coach UI components (grid, card, heatmap)</name>
  <files>src/components/coach/user-card-grid.tsx, src/components/coach/user-card.tsx, src/components/coach/habit-heatmap.tsx</files>
  <read_first>
    - src/lib/queries/coach.ts (CoachUserCard and HabitDay interfaces from Plan 01)
    - src/lib/actions/coach.ts (fetchUserDetail server action from Plan 01)
    - src/lib/actions/social.ts (sendNudge function signature)
    - src/components/ui/pixel-card.tsx (PixelCard component)
    - src/components/ui/pixel-button.tsx (PixelButton component)
    - src/components/animations/streak-pulse.tsx (pulse animation pattern for red hearts)
    - DESIGN-SPEC.md (visual identity, colors, component rules)
    - src/app/(coach)/layout.tsx (coach layout shell for context)
  </read_first>
  <action>
Create three client components in src/components/coach/:

**1. src/components/coach/habit-heatmap.tsx** ("use client")
- Props: { data: HabitDay[] } -- import HabitDay from "@/lib/queries/coach"
- Render a grid: each row = one habit (habitName label + habitIcon if available), each column = one day (Mon through Sun)
- Column headers: M, T, W, T, F, S, S (single-letter day abbreviations)
- Each cell is a 24x24 pixel block: completed = bg with var(--color-success), missed = bg with var(--color-surface-raised)
- Use squared edges (no rounded corners), 2px gap between cells
- Habit name column on the left, truncated to ~8 chars with ellipsis if needed
- Font: font-body text-[10px] for labels
- If data is empty, show "No habit data" in text-text-dim

**2. src/components/coach/user-card.tsx** ("use client")
- Props: { user: CoachUserCard } -- import CoachUserCard from "@/lib/queries/coach"
- Uses PixelCard as the container, add className="cursor-pointer" and onClick to toggle expand
- **Card summary (always visible):**
  - Top row: displayName (font-heading text-[12px] text-primary) on the left, risk heart icon on the right (top-right position)
  - Risk heart: <motion.img> with src={`/assets/icon-heart-${user.riskLevel}.svg`} width={24} height={24}
  - If riskLevel === "red", animate scale: [1, 1.05, 1] with repeat: Infinity, duration: 2 (same as StreakFlame pattern)
  - Second row: streak flame icon (img src="/assets/icon-flame.svg" width=16 height=16) + streak count (font-heading text-[11px]), weekly XP (font-body text-[11px] text-accent), last active relative time (font-body text-[10px] text-text-dim)
  - Format last active using formatDistanceToNow from "date-fns" with { addSuffix: true }. If null, show "Never"
- **Expanded section (AnimatePresence + motion.div):**
  - Use useState<boolean> for expanded state, useState<HabitDay[] | null> for heatmap data
  - On first expand, call fetchUserDetail(user.userId) via startTransition from useTransition, store result
  - Cache heatmap data -- don't re-fetch on subsequent toggles
  - Render HabitHeatmap component with fetched data
  - Render "Send Nudge" PixelButton (variant="secondary", size="sm") with nudge icon (img src="/assets/icon-nudge.svg" width=14 height=14)
  - Nudge button: onClick calls sendNudge(user.userId) via startTransition, sets local nudgedToday state to true on success
  - Nudge button disabled={user.nudgedToday || localNudgedToday} with text changing to "NUDGED" when disabled
  - AnimatePresence wraps expanded content: initial={{ height: 0, opacity: 0 }}, animate={{ height: "auto", opacity: 1 }}, exit={{ height: 0, opacity: 0 }}, transition={{ duration: 0.2 }}, className="overflow-hidden"
  - Show loading state while heatmap fetches: "Loading..." in text-text-dim
  - Stop click propagation on nudge button so it doesn't collapse the card

**3. src/components/coach/user-card-grid.tsx** ("use client")
- Props: { users: CoachUserCard[] } -- import CoachUserCard from "@/lib/queries/coach"
- Render CSS grid: grid-cols-1 on smallest screens, grid-cols-2 on sm (640px+), grid-cols-3 on md (768px+)
- Gap: gap-4
- Map over users array, render <UserCard user={user} key={user.userId} /> for each
- If users array is empty, render an empty state: PixelCard with "No users to monitor" text in text-text-dim, centered

All components use "use client" directive. Import motion and AnimatePresence from "motion/react". Use Tailwind classes matching existing design system (bg-surface, text-primary, font-heading, font-body, border-3, etc).
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/streakengine && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/coach/user-card-grid.tsx exists with "use client" directive
    - File src/components/coach/user-card.tsx exists with "use client" directive
    - File src/components/coach/habit-heatmap.tsx exists with "use client" directive
    - grep "grid-cols-2" src/components/coach/user-card-grid.tsx returns match (responsive grid)
    - grep "grid-cols-3" src/components/coach/user-card-grid.tsx returns match (3 cols desktop)
    - grep "icon-heart-" src/components/coach/user-card.tsx returns match (risk heart icons)
    - grep "AnimatePresence" src/components/coach/user-card.tsx returns match (accordion expand)
    - grep "fetchUserDetail" src/components/coach/user-card.tsx returns match (lazy-load heatmap)
    - grep "sendNudge" src/components/coach/user-card.tsx returns match (nudge action)
    - grep "useTransition" src/components/coach/user-card.tsx returns match (non-blocking actions)
    - grep "scale.*1.*1.05.*1" src/components/coach/user-card.tsx returns match (red heart pulse)
    - grep "color-success" src/components/coach/habit-heatmap.tsx returns match (completed block color)
    - grep "color-surface-raised" src/components/coach/habit-heatmap.tsx returns match (missed block color)
    - npm run build passes
  </acceptance_criteria>
  <done>Three coach components exist: UserCardGrid (responsive grid), UserCard (summary + accordion expand with heatmap + nudge), HabitHeatmap (7-day habit completion grid). Red hearts pulse. Nudge disabled after sending. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire dashboard page as server component</name>
  <files>src/app/(coach)/dashboard/page.tsx</files>
  <read_first>
    - src/app/(coach)/dashboard/page.tsx (existing placeholder to replace)
    - src/app/(coach)/layout.tsx (coach layout context)
    - src/lib/queries/coach.ts (getCoachDashboardData)
    - src/lib/auth.ts (requireAuth for session)
    - src/components/coach/user-card-grid.tsx (UserCardGrid component)
  </read_first>
  <action>
Replace the placeholder src/app/(coach)/dashboard/page.tsx with a server component:

- Import requireAuth from "@/lib/auth"
- Import getCoachDashboardData from "@/lib/queries/coach"
- Import UserCardGrid from "@/components/coach/user-card-grid"

The default export async function CoachDashboardPage():
1. Call requireAuth() to get session (also provides coach role verification via layout)
2. Call getCoachDashboardData(session.userId) to fetch all user data with risk classification
3. Compute summary stats for a header: totalUsers = users.length, atRisk = users.filter(u => u.riskLevel === "red").length, moderate = users.filter(u => u.riskLevel === "yellow").length
4. Render layout:
   - Header section with title "COMMUNITY" in font-heading text-[16px] text-primary
   - Stats row below title showing: "{totalUsers} USERS" | "{atRisk} AT RISK" (text-danger) | "{moderate} MODERATE" (text-warning/text-accent)
   - Stats use font-body text-[11px] with a gap-4 flex row
   - Render <UserCardGrid users={users} /> below the header
   - Wrap everything in a flex flex-col gap-6 py-4 container

Keep it minimal -- server component fetches data, passes to client grid component. No "use client" directive (this is a server component).
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/streakengine && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File src/app/(coach)/dashboard/page.tsx exists and does NOT contain "use client"
    - grep "getCoachDashboardData" src/app/(coach)/dashboard/page.tsx returns match
    - grep "requireAuth" src/app/(coach)/dashboard/page.tsx returns match
    - grep "UserCardGrid" src/app/(coach)/dashboard/page.tsx returns match
    - grep "COMMUNITY" src/app/(coach)/dashboard/page.tsx returns match (page title)
    - grep "AT RISK" src/app/(coach)/dashboard/page.tsx returns match (risk counter)
    - npm run build passes
  </acceptance_criteria>
  <done>Coach dashboard page is a server component that fetches engagement data and renders UserCardGrid. Shows community header with risk summary counts. Build passes. Full coach experience is functional.</done>
</task>

</tasks>

<verification>
- npm run build passes with no errors
- Coach dashboard renders grid with user cards sorted by risk level
- Red heart icons pulse, yellow and green are static
- Clicking a card expands to show habit heatmap
- Nudge button inside expanded card sends nudge and disables
- Summary stats show total, at-risk, and moderate user counts
- Grid is 2 columns on mobile, 3 on desktop
</verification>

<success_criteria>
Build passes. Coach can see user cards sorted by risk (red/yellow/green hearts), expand cards to see 7-day habit heatmap, and send nudges from expanded cards. All 5 COCH requirements are visually and functionally delivered.
</success_criteria>

<output>
After completion, create `.planning/phases/04-coach-dashboard/04-02-SUMMARY.md`
</output>
