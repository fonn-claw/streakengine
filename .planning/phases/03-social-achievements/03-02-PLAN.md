---
phase: 03-social-achievements
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/leaderboard-peek.tsx
  - src/components/leaderboard-full.tsx
  - src/components/friend-row.tsx
  - src/components/friends-list.tsx
  - src/components/nudge-toast.tsx
  - src/components/challenge-progress.tsx
  - src/app/(game)/page.tsx
  - src/app/(game)/friends/page.tsx
  - src/app/(game)/quests/page.tsx
autonomous: true
requirements:
  - LDBD-01
  - LDBD-03
  - LDBD-04
  - SOCL-01
  - SOCL-04

must_haves:
  truths:
    - "Home page shows compact top-3 leaderboard peek below the quest board"
    - "Leaderboard rows show rank, trophy icon for top 3, display name, weekly XP, and current user highlighted"
    - "Friends page shows list of friends with streak, level, last active, and nudge button"
    - "Nudge button sends nudge and shows sent confirmation; disabled after sending"
    - "Quests page shows full leaderboard with all friends ranked"
    - "Weekly challenge card shows participant progress with blocky progress blocks"
    - "Unread nudges show as toast notification on friends page"
  artifacts:
    - path: "src/components/leaderboard-peek.tsx"
      provides: "Compact 3-row leaderboard for home page"
      contains: "icon-trophy-gold"
    - path: "src/components/leaderboard-full.tsx"
      provides: "Full ranked leaderboard for quests page"
      contains: "border-l-4"
    - path: "src/components/friend-row.tsx"
      provides: "Single friend PixelCard row with nudge button"
      contains: "sendNudge"
    - path: "src/components/friends-list.tsx"
      provides: "Friends list container with nudge toast"
      contains: "FriendRow"
    - path: "src/components/challenge-progress.tsx"
      provides: "Weekly challenge card with participant progress blocks"
      contains: "targetXp"
    - path: "src/app/(game)/page.tsx"
      provides: "Home page with leaderboard peek"
      contains: "LeaderboardPeek"
    - path: "src/app/(game)/friends/page.tsx"
      provides: "Friends page with friend list and nudge toast"
      contains: "FriendsList"
    - path: "src/app/(game)/quests/page.tsx"
      provides: "Quests page with full leaderboard"
      contains: "LeaderboardFull"
  key_links:
    - from: "src/app/(game)/page.tsx"
      to: "src/lib/queries/leaderboard.ts"
      via: "getWeeklyLeaderboard called in server component"
      pattern: "getWeeklyLeaderboard"
    - from: "src/components/friend-row.tsx"
      to: "src/lib/actions/social.ts"
      via: "sendNudge server action on button click"
      pattern: "sendNudge"
    - from: "src/app/(game)/friends/page.tsx"
      to: "src/lib/queries/friends.ts"
      via: "getFriendsList + getUnreadNudges in server component"
      pattern: "getFriendsList"
---

<objective>
Build leaderboard components (home peek + full), friends page with nudge mechanic, challenge progress card, and nudge toast. Wire into home, friends, and quests pages.

Purpose: Delivers the social competition layer -- users see weekly rankings, friend activity, and can send nudges. This is the accountability engine that drives retention.
Output: 6 new components and 3 wired pages.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-social-achievements/03-CONTEXT.md
@.planning/phases/03-social-achievements/03-RESEARCH.md
@.planning/phases/03-social-achievements/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (queries and actions) -->

From src/lib/queries/leaderboard.ts:
```typescript
export async function getWeeklyLeaderboard(userId: number, timezone: string): Promise<{
  rank: number; userId: number; displayName: string; weeklyXp: number; isCurrentUser: boolean;
}[]>;
```

From src/lib/queries/friends.ts:
```typescript
export async function getFriendsList(userId: number): Promise<{
  userId: number; displayName: string; level: number; totalXp: number;
  currentStreak: number; lastActive: Date | null;
}[]>;
export async function getFriendCount(userId: number): Promise<number>;
export async function getUnreadNudges(userId: number): Promise<{
  id: number; senderName: string; date: string;
}[]>;
```

From src/lib/queries/challenges.ts:
```typescript
export async function getActiveChallenge(userId: number, timezone: string): Promise<{
  challenge: { id: number; name: string; description: string | null; targetXp: number; startDate: string; endDate: string; } | null;
  participants: { userId: number; displayName: string; currentXp: number; }[];
}>;
```

From src/lib/actions/social.ts:
```typescript
export async function sendNudge(friendId: number): Promise<{ sent: boolean }>;
export async function markNudgesRead(nudgeIds: number[]): Promise<void>;
```

<!-- Existing components to follow/reuse -->

From src/components/ui/pixel-card.tsx:
```typescript
// PixelCard component with bg-surface, pixel-border styling
```

From src/components/ui/pixel-button.tsx:
```typescript
// PixelButton with primary/secondary/danger variants, sm/md/lg sizes
```

From src/components/xp-bar.tsx:
```typescript
export function XPBar({ current, total, segments }: { current: number; total: number; segments?: number }): JSX.Element;
```

From src/components/quest-board.tsx:
```typescript
// QuestBoard uses server component -> client island pattern
// Server page fetches data, passes as props to client QuestBoard
```

From src/app/(game)/page.tsx (current):
```typescript
// Server component that fetches user, quests, streakState
// Renders <QuestBoard quests={quests} multiplier={...} streakState={...} level={...} totalXp={...} />
```

<!-- Asset paths -->
Assets: icon-trophy-gold.svg, icon-trophy-silver.svg, icon-trophy-bronze.svg, icon-crown.svg,
        icon-people.svg, icon-nudge.svg, empty-friends.svg, empty-quests.svg
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Leaderboard components and challenge progress card</name>
  <files>
    src/components/leaderboard-peek.tsx,
    src/components/leaderboard-full.tsx,
    src/components/challenge-progress.tsx
  </files>
  <read_first>
    src/components/ui/pixel-card.tsx,
    src/components/ui/pixel-button.tsx,
    src/components/xp-bar.tsx,
    src/components/quest-card.tsx,
    src/app/(game)/page.tsx,
    src/lib/queries/leaderboard.ts,
    src/lib/queries/challenges.ts,
    DESIGN-SPEC.md
  </read_first>
  <action>
**1. Create `src/components/leaderboard-peek.tsx`** ("use client"):
- Props: `entries: { rank: number; userId: number; displayName: string; weeklyXp: number; isCurrentUser: boolean }[]`
- Show top 3 entries only (slice to 3).
- If current user is NOT in top 3, show their row as 4th row with a divider line.
- Each row: rank number (font-heading), trophy icon for ranks 1-3 (img src="/assets/icon-trophy-gold.svg" for rank 1, silver for 2, bronze for 3, width=16 height=16), display name (font-body, truncate), weekly XP value right-aligned (font-heading, text-secondary).
- Rank 1 also gets icon-crown.svg (width=12 height=12) next to name.
- Current user's row: `border-l-4` with `border-primary` (--color-primary), `bg-surface-raised` background.
- Wrap in PixelCard. Header: "WEEKLY BOARD" in font-heading text-[11px].
- Below the list: `<p className="font-body text-text-dim text-[10px] mt-2">Resets Monday</p>`.
- Empty state: if entries is empty, show empty-quests.svg with "No competition yet" text.

**2. Create `src/components/leaderboard-full.tsx`** ("use client"):
- Props: same as peek but show ALL entries (no slice).
- Same row rendering as peek but with more vertical spacing.
- Current user row highlighted identically (border-l-4 border-primary bg-surface-raised).
- Trophy icons for top 3, crown for #1.
- "Resets Monday" text at bottom.
- Wrap in div (not PixelCard -- it's the page content).
- Empty state: empty-quests.svg with "No friends to compete with" text.

**3. Create `src/components/challenge-progress.tsx`** ("use client"):
- Props: `challenge: { id: number; name: string; description: string | null; targetXp: number } | null, participants: { userId: number; displayName: string; currentXp: number }[]`
- If challenge is null, return null (don't render).
- Wrap in PixelCard with bg-surface.
- Header: challenge.name in font-heading text-[11px] text-primary.
- For each participant: row with displayName (font-body, text-[11px]), then a segmented progress bar.
- Progress bar: reuse XPBar pattern from xp-bar.tsx. Pass current=currentXp, total=targetXp, segments=10.
- Show "currentXp / targetXp XP" text right-aligned in font-heading text-[10px] text-secondary.
- Sort participants by currentXp DESC.
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/streakengine && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/leaderboard-peek.tsx contains `"use client"` and `icon-trophy-gold` and `icon-trophy-silver` and `icon-trophy-bronze`
    - src/components/leaderboard-peek.tsx contains `icon-crown.svg`
    - src/components/leaderboard-peek.tsx contains `border-l-4` and `border-primary`
    - src/components/leaderboard-peek.tsx contains `Resets Monday`
    - src/components/leaderboard-full.tsx contains `"use client"` and `icon-trophy-gold`
    - src/components/leaderboard-full.tsx contains `border-l-4` and `border-primary`
    - src/components/challenge-progress.tsx contains `"use client"` and `targetXp`
    - src/components/challenge-progress.tsx contains `XPBar` or segmented progress rendering
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>LeaderboardPeek shows top 3 with trophies and current user highlight. LeaderboardFull shows all ranked friends. ChallengeProgress shows participant progress blocks. All components build cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Friends list, nudge mechanics, and page wiring</name>
  <files>
    src/components/friend-row.tsx,
    src/components/friends-list.tsx,
    src/components/nudge-toast.tsx,
    src/app/(game)/page.tsx,
    src/app/(game)/friends/page.tsx,
    src/app/(game)/quests/page.tsx
  </files>
  <read_first>
    src/components/ui/pixel-card.tsx,
    src/components/ui/pixel-button.tsx,
    src/components/leaderboard-peek.tsx,
    src/components/leaderboard-full.tsx,
    src/components/challenge-progress.tsx,
    src/components/quest-board.tsx,
    src/app/(game)/page.tsx,
    src/app/(game)/friends/page.tsx,
    src/app/(game)/quests/page.tsx,
    src/lib/queries/friends.ts,
    src/lib/queries/leaderboard.ts,
    src/lib/queries/challenges.ts,
    src/lib/actions/social.ts,
    src/lib/auth.ts,
    DESIGN-SPEC.md
  </read_first>
  <action>
**1. Create `src/components/nudge-toast.tsx`** ("use client"):
- Props: `nudges: { id: number; senderName: string; date: string }[], onDismiss: () => void`
- If nudges.length === 0, return null.
- Render a fixed-position toast at top of screen (or a banner at top of page content).
- Show icon-nudge.svg (width=20 height=20) + "senderName nudged you!" text in font-body text-[11px].
- If multiple nudges, show count: "You have {N} nudges!" with latest sender name.
- PixelButton (sm, secondary) "OK" to dismiss.
- On dismiss, call markNudgesRead server action with all nudge IDs, then call onDismiss callback.

**2. Create `src/components/friend-row.tsx`** ("use client"):
- Props: `friend: { userId: number; displayName: string; level: number; totalXp: number; currentStreak: number; lastActive: Date | null }`
- Wrap in PixelCard.
- Layout: horizontal row with:
  - Left: displayName (font-body text-[12px] text-text), below it "Lvl {level}" (font-heading text-[10px] text-text-dim)
  - Middle: flame icon (small, inline) + currentStreak count (font-heading text-[11px] text-secondary)
  - Right: PixelButton (sm, secondary) with icon-nudge.svg and text "NUDGE"
- Nudge button: on click, call `sendNudge(friend.userId)`. Use useState for sent state. If sent, disable button and change text to "SENT!" with a green color. If sendNudge returns `{ sent: false }` (already nudged today), show "SENT!" too.
- Last active: show relative time below streak count ("Active 2h ago" or "Active yesterday") in text-text-dim text-[10px]. Use simple logic: if today show "Today", if yesterday show "Yesterday", else show date.

**3. Create `src/components/friends-list.tsx`** ("use client"):
- Props: `friends: { userId: number; displayName: string; level: number; totalXp: number; currentStreak: number; lastActive: Date | null }[], nudges: { id: number; senderName: string; date: string }[]`
- Render NudgeToast at top if nudges exist. Track dismissed state.
- Render FriendRow for each friend.
- Empty state: if friends.length === 0, show empty-friends.svg (width=200 height=160) centered with "No party members yet" text in font-body text-text-dim text-[12px].

**4. Modify `src/app/(game)/page.tsx`** (server component):
- KEEP existing QuestBoard rendering.
- Add imports: `getWeeklyLeaderboard` from queries/leaderboard, `getActiveChallenge` from queries/challenges.
- Add to Promise.all: `getWeeklyLeaderboard(session.userId, user.timezone)`, `getActiveChallenge(session.userId, user.timezone)`.
- After `<QuestBoard>`, add `<LeaderboardPeek entries={leaderboard} />` and `<ChallengeProgress challenge={challenge?.challenge ?? null} participants={challenge?.participants ?? []} />`.
- Import LeaderboardPeek and ChallengeProgress components.

**5. Replace `src/app/(game)/friends/page.tsx`** (server component):
- Import requireAuth, db, users, eq.
- Import getFriendsList, getUnreadNudges from queries/friends.
- Fetch user, then parallel fetch getFriendsList + getUnreadNudges.
- Render page title "FRIENDS" in font-heading text-[14px] text-primary.
- Render `<FriendsList friends={friends} nudges={nudges} />`.

**6. Replace `src/app/(game)/quests/page.tsx`** (server component):
- Import requireAuth, db, users, eq.
- Import getWeeklyLeaderboard from queries/leaderboard.
- Fetch user, then getWeeklyLeaderboard.
- Render page title "LEADERBOARD" in font-heading text-[14px] text-primary.
- Render `<LeaderboardFull entries={leaderboard} />`.
- Below leaderboard, could also show today's quests (import getTodayQuests and QuestBoard) or keep simple with just leaderboard.
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/streakengine && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/friend-row.tsx contains `"use client"` and `sendNudge` and `NUDGE` and `SENT!`
    - src/components/friend-row.tsx contains `icon-nudge.svg`
    - src/components/friends-list.tsx contains `"use client"` and `FriendRow` and `NudgeToast`
    - src/components/friends-list.tsx contains `empty-friends.svg`
    - src/components/nudge-toast.tsx contains `"use client"` and `markNudgesRead`
    - src/app/(game)/page.tsx contains `LeaderboardPeek` and `getWeeklyLeaderboard`
    - src/app/(game)/page.tsx contains `ChallengeProgress` and `getActiveChallenge`
    - src/app/(game)/page.tsx contains `QuestBoard` (existing, preserved)
    - src/app/(game)/friends/page.tsx contains `getFriendsList` and `getUnreadNudges` and `FriendsList`
    - src/app/(game)/quests/page.tsx contains `getWeeklyLeaderboard` and `LeaderboardFull`
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Friends list with nudge buttons, nudge toast, home page with leaderboard peek + challenge card, friends page with full friend list, quests page with full leaderboard. All pages wired to server-side data. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` passes
- Home page renders QuestBoard + LeaderboardPeek + ChallengeProgress
- Friends page renders FriendsList with nudge capability
- Quests page renders full leaderboard
- Nudge button calls sendNudge action and shows sent state
- Trophy icons appear for top 3 leaderboard positions
- Current user row highlighted with border-l-4 border-primary
</verification>

<success_criteria>
- 6 new components created with pixel-art styling
- 3 pages wired to real data from Plan 01 queries
- Leaderboard peek on home page shows top 3 with trophy icons
- Full leaderboard on quests page shows all friends ranked
- Friends list shows streak/level/nudge for each friend
- Nudge toast shows unread nudges on friends page
- Challenge progress shows participant blocks
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-social-achievements/03-02-SUMMARY.md`
</output>
