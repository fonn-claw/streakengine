---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(game)/layout.tsx
  - src/app/(game)/page.tsx
  - src/app/(coach)/layout.tsx
  - src/app/(coach)/dashboard/page.tsx
  - src/components/ui/pixel-button.tsx
  - src/components/ui/pixel-card.tsx
  - src/components/ui/bottom-nav.tsx
  - src/components/ui/top-nav.tsx
autonomous: true
requirements:
  - UIDN-01
  - UIDN-02
  - UIDN-03
  - UIDN-04
  - UIDN-05
  - UIDN-06

must_haves:
  truths:
    - "All UI uses pixel-art aesthetic: squared edges, thick 3px borders, bold saturated colors"
    - "Background is dark navy #1A1A2E everywhere, no light mode"
    - "Headings and numbers use Press Start 2P font; body text uses Silkscreen font"
    - "Mobile view shows bottom tab bar with 4 items using pixel-art SVG icons from public/assets/"
    - "Desktop view shows top nav strip with content max-width 720px centered"
    - "Login page shows hero-login.png, logo.svg wordmark, pixel-styled form with bg-pattern.png background"
    - "Pre-generated SVG icons from public/assets/ are used for nav tabs"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme tokens with all design-spec colors, pixel font variables, border-radius:0 global"
      contains: "@theme"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Press Start 2P and Silkscreen fonts via next/font/google"
      contains: "Press_Start_2P"
    - path: "src/components/ui/pixel-button.tsx"
      provides: "Squared pixel-art button with 3px border"
      contains: "pixel-border"
    - path: "src/components/ui/pixel-card.tsx"
      provides: "Squared pixel-art card/panel component"
      contains: "pixel-border"
    - path: "src/components/ui/bottom-nav.tsx"
      provides: "Mobile bottom tab bar with 4 pixel-art SVG icon tabs"
      contains: "icon-flame.svg"
    - path: "src/components/ui/top-nav.tsx"
      provides: "Desktop top nav strip"
      contains: "icon-flame.svg"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Pixel-art styled login page with hero image and form"
      contains: "hero-login.png"
  key_links:
    - from: "src/app/globals.css"
      to: "DESIGN-SPEC.md"
      via: "All color tokens match design spec hex values"
      pattern: "--color-bg.*#1A1A2E"
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "Font CSS variables consumed by @theme"
      pattern: "--font-press-start"
    - from: "src/components/ui/bottom-nav.tsx"
      to: "public/assets/"
      via: "SVG icon imports for nav tabs"
      pattern: "/assets/icon-flame\\.svg"
---

<objective>
Build the pixel-art design system (Tailwind v4 tokens, fonts, global styles), core UI components (PixelButton, PixelCard, BottomNav, TopNav), style the login page with hero image, and create the responsive game/coach layout shells.

Purpose: Establish the complete visual identity so every subsequent phase builds on the pixel-art game aesthetic. Users see a game, not a tool.
Output: A fully styled app shell where login looks like launching a game, navigation uses pixel-art icons, and the layout adapts between mobile tab bar and desktop top nav.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@DESIGN-SPEC.md
@BRIEF.md

<interfaces>
<!-- From Plan 01 -- auth types and actions the UI depends on -->

From src/lib/auth.ts:
```typescript
export interface SessionData {
  userId: number;
  role: "player" | "coach";
  displayName: string;
  isLoggedIn: boolean;
}
export async function getSession(): Promise<IronSession<SessionData>>;
export async function requireAuth(): Promise<IronSession<SessionData>>;
```

From src/lib/actions/auth.ts:
```typescript
export async function login(prevState: unknown, formData: FormData): Promise<{ error?: string }>;
export async function logout(): never; // redirects
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design system -- Tailwind v4 tokens, global styles, fonts, and core UI components</name>
  <files>src/app/globals.css, src/app/layout.tsx, src/components/ui/pixel-button.tsx, src/components/ui/pixel-card.tsx</files>
  <read_first>
    DESIGN-SPEC.md
    .planning/phases/01-foundation/01-CONTEXT.md
    .planning/phases/01-foundation/01-RESEARCH.md
    src/app/globals.css
    src/app/layout.tsx
  </read_first>
  <action>
1. Rewrite `src/app/globals.css` with Tailwind v4 CSS-first config:
```css
@import "tailwindcss";

@theme {
  --color-bg: #1A1A2E;
  --color-surface: #16213E;
  --color-surface-raised: #0F3460;
  --color-border: #533483;
  --color-primary: #E94560;
  --color-secondary: #FFD460;
  --color-accent: #00D2FF;
  --color-text: #EAEAEA;
  --color-text-dim: #8B8B9E;
  --color-success: #4ADE80;
  --color-warning: #FBBF24;
  --color-danger: #F43F5E;
  --color-xp-bar: #A855F7;

  --font-heading: var(--font-press-start);
  --font-body: var(--font-silkscreen);
}

/* Kill all rounded corners globally -- pixel art aesthetic */
* {
  border-radius: 0 !important;
}

/* Pixel border utility */
.pixel-border {
  border: 3px solid var(--color-border);
}

/* Body defaults */
body {
  background-color: var(--color-bg);
  color: var(--color-text);
  font-family: var(--font-body), monospace;
}

/* Heading font utility */
.font-pixel {
  font-family: var(--font-heading), monospace;
}

/* Scrollbar styling for dark theme */
::-webkit-scrollbar {
  width: 8px;
}
::-webkit-scrollbar-track {
  background: var(--color-bg);
}
::-webkit-scrollbar-thumb {
  background: var(--color-border);
}

/* Tileable background pattern */
.bg-game {
  background-image: url('/assets/bg-pattern.png');
  background-repeat: repeat;
  background-size: 256px 256px;
}
```

2. Rewrite `src/app/layout.tsx`:
- Import Press_Start_2P (weight "400") and Silkscreen (weights ["400", "700"]) from "next/font/google"
- Set CSS variables: --font-press-start and --font-silkscreen
- Apply both font variables to `<html>` className
- Set `<html lang="en" className={...}>` with dark background
- Import globals.css
- Set metadata: title "StreakEngine", description "Habit & Health Gamification Platform"
- Set favicon to /assets/favicon.svg

3. Create `src/components/ui/pixel-button.tsx`:
- Props: children, variant ("primary" | "secondary" | "danger", default "primary"), size ("sm" | "md" | "lg", default "md"), className, plus all button HTML attributes
- Styling: 3px solid border, no rounded corners, font-family inherits Silkscreen
- Variant colors:
  - primary: bg-primary text-white, border-color --color-primary, hover: brightness increase
  - secondary: bg-surface-raised text-secondary, border-color --color-secondary
  - danger: bg-danger text-white, border-color --color-danger
- Sizes: sm (px-3 py-1 text-xs), md (px-4 py-2 text-sm), lg (px-6 py-3 text-base)
- Active state: translate-y-[1px] for button press feel
- Use clsx for className merging
- Export as default and named export

4. Create `src/components/ui/pixel-card.tsx`:
- Props: children, className, as ("div" | "section", default "div")
- Styling: bg-surface, 3px solid --color-border, p-4
- Use clsx for className merging
- Export as default and named export

Verify `npm run build` passes after these changes.
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/streakengine && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/globals.css contains `@import "tailwindcss"` and `@theme` block
    - src/app/globals.css contains `--color-bg: #1A1A2E` and `--color-primary: #E94560` and `--color-surface: #16213E`
    - src/app/globals.css contains `border-radius: 0 !important`
    - src/app/globals.css contains `.pixel-border` with `border: 3px solid`
    - src/app/globals.css contains `--font-heading: var(--font-press-start)` and `--font-body: var(--font-silkscreen)`
    - src/app/layout.tsx contains `Press_Start_2P` and `Silkscreen` imports from "next/font/google"
    - src/app/layout.tsx contains `--font-press-start` and `--font-silkscreen` CSS variable assignments
    - src/app/layout.tsx contains `favicon.svg`
    - src/components/ui/pixel-button.tsx contains `pixel-border` or `border: 3px` and `primary` and `clsx`
    - src/components/ui/pixel-card.tsx contains `pixel-border` or `border: 3px` and `color-surface`
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Design system tokens, global styles, pixel fonts, and core UI components (PixelButton, PixelCard) are complete and build passes</done>
</task>

<task type="auto">
  <name>Task 2: Responsive navigation (BottomNav + TopNav) and styled login page</name>
  <files>src/components/ui/bottom-nav.tsx, src/components/ui/top-nav.tsx, src/app/(auth)/login/page.tsx, src/app/(game)/layout.tsx, src/app/(game)/page.tsx, src/app/(coach)/layout.tsx, src/app/(coach)/dashboard/page.tsx</files>
  <read_first>
    DESIGN-SPEC.md
    .planning/phases/01-foundation/01-CONTEXT.md
    src/app/globals.css
    src/app/layout.tsx
    src/components/ui/pixel-button.tsx
    src/lib/auth.ts
    src/lib/actions/auth.ts
    src/app/(auth)/login/page.tsx
    src/app/(game)/layout.tsx
    src/app/(game)/page.tsx
    src/app/(coach)/layout.tsx
  </read_first>
  <action>
1. Create `src/components/ui/bottom-nav.tsx` (mobile bottom tab bar):
- Client component ("use client")
- Uses `usePathname()` from next/navigation to determine active tab
- 4 tabs with pixel-art SVG icons from public/assets/:
  - Home: `/assets/icon-flame.svg`, href "/"
  - Quests: `/assets/icon-sword.svg`, href "/quests"
  - Friends: `/assets/icon-people.svg`, href "/friends"
  - Profile: `/assets/icon-shield.svg`, href "/profile"
- Fixed at bottom, full width, bg-surface, 3px top border with --color-border
- Active tab: icon with CSS filter or wrapper div colored --color-primary, 2px bottom border --color-primary
- Inactive: --color-text-dim
- Labels in Silkscreen font, 10px, below icons
- Icons: 24x24 `<img>` tags referencing /assets/ paths
- Use Next.js `<Link>` for navigation (instant content swap, no page transitions)
- Only visible on mobile: `md:hidden` (Tailwind breakpoint)

2. Create `src/components/ui/top-nav.tsx` (desktop top nav strip):
- Client component ("use client")
- Uses `usePathname()` for active state
- Same 4 tabs as bottom nav, same icons, same hrefs
- Horizontal layout, centered, bg-surface, 3px bottom border --color-border
- Active tab: text --color-primary with 2px bottom border
- Logo on the left: `<img src="/assets/logo.svg" />` linked to "/"
- Logout button on the right using the `logout` server action (import from @/lib/actions/auth)
- Only visible on desktop: `hidden md:flex`
- Content area below nav: max-width 720px, mx-auto, px-4

3. Rewrite `src/app/(auth)/login/page.tsx` with full pixel-art styling:
- bg-pattern.png as tileable background on the page wrapper (bg-game class)
- hero-login.png displayed as large decorative image above or behind the form area (max-width 400px, centered)
- logo.svg wordmark ("STREAK ENGINE") centered above the form
- Form:
  - Email input: bg-surface, 3px solid --color-border, text-text, Silkscreen font, placeholder --color-text-dim, px-4 py-3
  - Password input: same styling
  - Labels: Silkscreen font, text-text-dim, text-xs, uppercase
  - Submit button: PixelButton variant="primary", full width, text "LOG IN" in pixel font
  - Error display: 3px border --color-danger, bg-surface, text-danger, Silkscreen font
- Uses `useActionState` (from "react") with the login server action
- Form uses `action={formAction}` pattern (not onSubmit)
- Pending state: button shows "LOADING..." and is disabled
- Center the form vertically and horizontally on the page, max-width 380px

4. Rewrite `src/app/(game)/layout.tsx`:
- Server component
- Import getSession from @/lib/auth
- Get session data for passing to nav components (displayName, role)
- Render TopNav (desktop) + main content area (max-w-[720px] mx-auto px-4 pb-20 md:pb-4) + BottomNav (mobile)
- The pb-20 on mobile ensures content does not hide behind the fixed bottom nav

5. Update `src/app/(game)/page.tsx`:
- Show a pixel-art styled "empty state" placeholder for the quest board
- Use PixelCard component
- Heading "QUEST BOARD" in Press Start 2P font
- Message: "Your daily quests will appear here" in Silkscreen
- Use empty-quests.svg as illustration
- This is a placeholder -- Phase 2 replaces it with the actual quest board

6. Rewrite `src/app/(coach)/layout.tsx`:
- Server component with session check
- Similar structure to game layout but with coach-specific nav
- TopNav with logo + "Coach Dashboard" label + logout button
- No bottom tab bar (coaches use desktop primarily)
- Content max-width 720px centered

7. Update `src/app/(coach)/dashboard/page.tsx`:
- Pixel-art styled placeholder
- Heading "COACH DASHBOARD" in Press Start 2P
- Message: "Community engagement data will appear here"
- Use PixelCard component

IMPORTANT:
- All SVG icons from public/assets/ are used via `<img src="/assets/icon-name.svg" />` -- they are static assets, not React components
- No rounded corners anywhere (global CSS handles this)
- No page transitions between tabs -- instant swap via Next.js Link
- Nav does NOT show streak/XP summary -- that is the hero banner's job (Phase 2)
- Active tab indicator: --color-primary icon color + 2px bottom border (per DESIGN-SPEC.md and CONTEXT.md)
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/streakengine && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/bottom-nav.tsx contains `"use client"` and `usePathname` and `/assets/icon-flame.svg` and `/assets/icon-sword.svg` and `/assets/icon-people.svg` and `/assets/icon-shield.svg`
    - src/components/ui/bottom-nav.tsx contains `md:hidden`
    - src/components/ui/top-nav.tsx contains `"use client"` and `usePathname` and `/assets/logo.svg` and `hidden md:flex` or `hidden md:block`
    - src/components/ui/top-nav.tsx contains `max-w` with `720` for content width constraint
    - src/app/(auth)/login/page.tsx contains `hero-login.png` and `logo.svg` and `bg-pattern.png` or `bg-game`
    - src/app/(auth)/login/page.tsx contains `useActionState` and `login`
    - src/app/(game)/layout.tsx contains `BottomNav` and `TopNav` and `getSession`
    - src/app/(game)/page.tsx contains `QUEST BOARD` or `Quest Board` and `empty-quests.svg`
    - src/app/(coach)/dashboard/page.tsx contains `COACH DASHBOARD` or `Coach Dashboard`
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Responsive navigation with pixel-art SVG icons works on mobile (bottom tabs) and desktop (top strip). Login page styled with hero image, logo, and pixel-art form. Game and coach layout shells complete with placeholders for Phase 2+ content.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. Login page shows hero-login.png, logo.svg, pixel-styled form with bg-pattern.png background
3. Mobile viewport shows bottom tab bar with 4 pixel-art SVG icons
4. Desktop viewport shows top nav strip with logo, tabs, and logout; content max-width 720px
5. All colors match DESIGN-SPEC.md tokens (--color-bg: #1A1A2E, --color-primary: #E94560, etc.)
6. Press Start 2P used for headings, Silkscreen for body text
7. No rounded corners visible anywhere
8. All border widths are 3px on cards and panels
</verification>

<success_criteria>
- Build succeeds with full pixel-art design system applied
- Login page looks like launching a game (hero image, pixel fonts, dark theme, squared inputs)
- Navigation uses all 4 pixel-art SVG icons from public/assets/
- Mobile shows bottom tab bar, desktop shows top nav strip
- Content area max-width 720px centered on desktop
- Game shell and coach shell are visually distinct
- PixelButton and PixelCard components are reusable for Phase 2+
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
